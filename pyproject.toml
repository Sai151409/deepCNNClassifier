[build-system]
requires = ['setuptools>=42.0', 'wheel']
build-backend = 'setuptools.build_meta'

[tootls.pytest.ini_options]
testpaths = [
    "test"
    ]

[tool.mypy]
mypay_path = 'src
ignore_missing_imports = true